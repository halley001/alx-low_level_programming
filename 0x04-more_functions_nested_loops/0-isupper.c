#include "main.h"
/**
 * _isupper - Entry point
 *@c: parameter for comparism
 * Return: 1 if uppercase and 0 if lowercase
 */

int _isupper(int c)
{
if ((c >= 'A') && (c <= 'Z'))

return (1);

return (0);
}
